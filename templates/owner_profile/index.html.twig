{% extends 'base.html.twig' %}

{% block title %}Hello OwnerProfileController!{% endblock %}

{% block body %}
<main class="owner-detail">
    <div class="container">

        <section>
            <h2>Information sur {{ host.familyName }}</h2>
            <div class="content">
                <a href="{{ path('public_owner_profile', {'id': host.id}) }}" class="avatar">
                    
                </a>
                <article>
                    <h3><a href="{{ path('public_owner_profile', {'id': host.id}) }}">{{ host }}</a></h3>
                    <div>
                        
                    </div>
                </article>
                <article>
                    <div>
                        <p>Notes globale :</p>
                        <p>Nombres d’évaluations :</p>
                        <p>Années d’expérience :</p>
                        <p>Statut :</p>
                        <p>Email :</p>
                        <p>Téléphone :</p>
                        <p>Langues :</p>
                        <p>Taux de réponse :</p>
                        <p>Délai de réponse :</p>
                    </div>
                    <div>
                        <p>****</p>
                        <p>****</p>
                        <p>****</p>
                        <p>{{ host.status }}</p>
                        <p>{{ host.email }}</p>
                        <p>{{ host.phoneNumber }}</p>
                        <p>{{ host.language }}</p>
                        <p>****</p>
                        <p>****</p>
                    </div>
                </article>
            </div>
        </section>

        <section>
            <h2>Annonces publiées par {{ host.familyName }}</h2>
            <div class="content">
                <div class="box1">
                    <div class="item">
                        <div>

                        </div>
                        <p>Garage - Rue de l’exemple 32, 4000</p>
                    </div>
                </div>
                <div class="box2">
                    <h3>Vous avez des questions ? Contactez {{ host.familyName }}</h3>
                    <textarea name="" id="" cols="30" rows="10"></textarea>
                    <button>Envoyer le message</button>
                </div>
            </div>
        </section>

        <section>
            <h2>Commentaires pour {{ host.familyName }}</h2>

            {{ dump(host, host.owner.first) }}
            {% for row in host.review %}
            {{ dump(row) }}
            {% endfor %}
        </section>
    </div>
</main>
{% endblock %}
