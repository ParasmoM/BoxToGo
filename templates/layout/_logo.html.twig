<div class="logo">
    <a href="{{ path('public_home') }}" class="logo-link">
        <img src="{{ asset('images/logo_boxtogo.svg') }}" alt="BoxToGo logo" class="logo-image">
    </a>

    {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
        <div class="admin-switch">
            <a href="{{ path('app_admin') }}" role="button" class="admin-switch__link">
                Admin Interface
            </a>
        </div>
    {% endif %}
</div>